<!DOCTYPE html>
<html>

<head>
    <title>Skylab Academy - Document Details</title>
    <link rel="stylesheet" href="/form.css">
    <link rel="icon" type="image/x-icon" href="/logo.ico">
    <style>
        .document-info {
            margin: 20px;
            padding: 20px;
            border: 1px solid #ccc;
            border-radius: 5px;
        }

        .info-group {
            margin-bottom: 15px;
        }

        .info-label {
            font-weight: bold;
            color: #333;
        }

        .info-value {
            margin-left: 10px;
            color: #666;
        }

        .id-card-images {
            display: flex;
            gap: 20px;
            margin: 20px 0;
        }

        .id-card-images img {
            max-width: 300px;
            border: 1px solid #ddd;
            border-radius: 5px;
        }

        .payment-plan {
            margin: 20px 0;
        }

        .payment-plan img {
            max-width: 500px;
            border: 1px solid #ddd;
            border-radius: 5px;
        }

        .back-button {
            margin: 20px;
            padding: 10px 20px;
            background-color: #007bff;
            color: white;
            border: none;
            border-radius: 5px;
            cursor: pointer;
        }

        .back-button:hover {
            background-color: #0056b3;
        }
    </style>
</head>

<body>
    <button class="back-button" onclick="window.history.back()">← Back to Dashboard</button>

    <div class="document-info">
        <h1>Document Details</h1>

        <div class="info-group">
            <span class="info-label">Agreement Number:</span>
            <span class="info-value">
                <%= document.agreementNumber %>
            </span>
        </div>

        <div class="info-group">
            <span class="info-label">Full Name:</span>
            <span class="info-value">
                <%= document.username %>
            </span>
        </div>

        <div class="info-group">
            <span class="info-label">TC Identity Number:</span>
            <span class="info-value">
                <%= document.tcno %>
            </span>
        </div>

        <div class="info-group">
            <span class="info-label">Email:</span>
            <span class="info-value">
                <%= document.email %>
            </span>
        </div>

        <div class="info-group">
            <span class="info-label">Birth Date:</span>
            <span class="info-value">
                <%= document.birthdate %>
            </span>
        </div>

        <div class="info-group">
            <span class="info-label">Address:</span>
            <span class="info-value">
                <%= document.adres %>
            </span>
        </div>

        <div class="info-group">
            <span class="info-label">Phone Number:</span>
            <span class="info-value">
                <%= document.phone %>
            </span>
        </div>

        <div class="info-group">
            <span class="info-label">Course:</span>
            <span class="info-value">
                <%= document.course %>
            </span>
        </div>

        <div class="info-group">
            <span class="info-label">Signed At:</span>
            <span class="info-value">
                <%= document.signedAt %>
            </span>
        </div>

        <h2>ID Card Images</h2>
        <div class="id-card-images">
            <div>
                <h3>Front Side</h3>
                <img src="<%= document.kimlikFront %>" alt="ID Card Front">
            </div>
            <div>
                <h3>Back Side</h3>
                <img src="<%= document.kimlikBack %>" alt="ID Card Back">
            </div>
        </div>

        <h2>Payment Plan</h2>
        <div class="payment-plan">
            <img src="<%= document.paymentPlan %>" alt="Payment Plan">
        </div>
    </div>
</body>

</html>